version: '2.1'

services:
  odc:
    image: odc
    build: .
    environment:
      NODE_ENV: development
    ports:
      - 4200:4200
      - 9229:9229
    command: node --inspect=0.0.0.0:9229 server.js